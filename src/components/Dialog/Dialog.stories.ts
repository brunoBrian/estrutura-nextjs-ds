import { ComponentMeta } from '@storybook/react';

import Dialog from '.';

export default {
  title: 'Dialog',
  component: Dialog
} as ComponentMeta<typeof Dialog>;

export const Primary: ComponentMeta<typeof Dialog> = {};
